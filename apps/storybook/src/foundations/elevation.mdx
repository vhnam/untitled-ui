import { Meta, Source } from "@storybook/blocks";
import tokens from "@repo/design-tokens";

import Card from "../components/Card";
import Elevation from "../components/Elevation";

<Meta title="Foundations/Elevation" />

# Elevation

<div
  style={{
    display: "grid",
    gridTemplateColumns: "repeat(3, 1fr)",
    gap: "1rem",

}}

> {Object.keys(tokens.elevation).map((elevationLevel) => (

    <Card
      key={elevationLevel}
      lead={elevationLevel}
      title={elevationLevel}
      content={<Elevation value={tokens.elevation[elevationLevel]} />}
    />

))}

</div>
