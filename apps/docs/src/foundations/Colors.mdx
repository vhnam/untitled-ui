import { Meta, Source } from '@storybook/blocks';
import tokens from '@repo/design-tokens';

import DesignSystemPalette from '../components/DesignSystemPalette';
import GradientColor from '../components/GradientColor';

<Meta title="Foundations/Colors" />

# Color Tokens

## Neutral

<DesignSystemPalette name="Neutral" colors={tokens.color.neutral} />

## Red

<DesignSystemPalette name="Red" colors={tokens.color.red} />

## Yellow

<DesignSystemPalette name="Yellow" colors={tokens.color.yellow} />

## Green

<DesignSystemPalette name="Green" colors={tokens.color.green} />

## Blue

<DesignSystemPalette name="Blue" colors={tokens.color.blue} />

## Gradient

### Psychology

<DesignSystemPalette
  name="Psychology"
  colors={tokens.color.gradient.psychology.map((color) => color.value)}
/>

### Biology

<DesignSystemPalette
  name="Biology"
  colors={tokens.color.gradient.biology.map((color) => color.value)}
/>

### Social

<DesignSystemPalette
  name="Social"
  colors={tokens.color.gradient.social.map((color) => color.value)}
/>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(3, 1fr)'
}}>

<div>
### Blue

<GradientColor colors={tokens.color.gradient.psychology.map((color) => color.value)} />
</div>

<div>
### Green

<GradientColor
  colors={tokens.color.gradient.biology.map((color) => color.value)}
/>

</div>

<div>
### Red

<GradientColor colors={tokens.color.gradient.social.map((color) => color.value)} />
</div>

</div>
